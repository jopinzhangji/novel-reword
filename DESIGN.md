# 自动写小说机器助手 — 设计文档

## 一、核心理念

- **多 Agent 协作**：不同 Agent 负责不同职责，通过编排器协调。
- **角色独立演进**：每个主要角色拥有自己的 Agent，独立记忆、目标与“思考”，随时间演进。
- **世界 = 整个故事范围**：一个世界对应一整部故事的宇宙（统一设定、总时间线、因果边界）。
- **世界内多条事件演进范围**：同一世界下可划分多个“范围”（如京城线、江湖线、宫廷线），每个范围有独立的事件演进，**可能独立、可能有联系**；每个范围也对应**多个世界层 Agent**（范围 Agent），而非单一世界 Agent。

---

## 二、世界与事件演进范围

| 概念 | 说明 |
|------|------|
| **世界 (World)** | **整个故事范围**。一部作品共享的：时代、规则、地图、全局时间轴、跨范围因果边界。 |
| **事件演进范围 (Scope)** | 世界内的一个“子范围”，可以按**地理**（京城/边关）、**势力**（宫廷/江湖/民间）、**情节线**（主线/某视角线）等划分。每个范围有自己的一批事件、局部状态、因果。 |
| **范围间关系** | **独立**：某段时间内两范围事件互不交叉，各自演进。**有联系**：共享事件、角色跨线、时间或因果交叉（如“京城出事”导致“江湖线”接到消息）。 |
| **世界层 Agent** | 每个事件演进范围可有一个 **范围 Agent**（子世界 Agent），负责该范围内的事件簿、因果、一致性、局部演进；另可设 **世界协调者 Agent**，管全局设定、统一时间、跨范围一致性与交叉。 |

**示例**：  
一个古代架空世界下，设三条范围——「京城·朝堂」「江湖·武林」「北疆·边关」。京城与江湖有联系（角色往返、情报互通），北疆可先独立演进，后期再与京城线交汇；每条范围一个范围 Agent，再加一个世界协调者统合时间与跨线事件。

---

## 三、整体架构

```
     ┌─────────────────────────────────────────────────────────────────────┐
     │  设定研究 Agent（特殊设定完整模型）· 设计期/按需调用                    │
     │  联网搜索 | 分析历史小说/网文 | 完善战力、等级、境界等配套设定 → 写入世界  │
     └──────────────────────────────────┬──────────────────────────────────┘
                                         │ 产出设定文档
                         ┌───────────────▼────────────────┐
                         │   编排器 (Orchestrator)            │
                         │  回合调度、冲突裁决、场景切分        │
                         └────────────┬───────────────────┘
                                       │
     ┌─────────────────────────────────┼─────────────────────────────────┐
     │ 角色层                            │ 世界层                            │
     ▼                                  ▼                                  │
┌──────────┐ ┌──────────┐    ┌──────────────────────────────────────────────┐
│ 角色 A   │ │ 角色 B   │ …  │  世界 (整个故事范围) · 含设定研究产出的特殊设定   │
│ 人格/记忆│ │ 人格/记忆│    │  ┌─────────────────────────────────────────┐  │
│ 目标/思考│ │ 目标/思考│    │  │ 世界协调者 Agent（可选）                   │  │
└────┬─────┘ └────┬─────┘    │  │ 全局设定、统一时间、跨范围因果与一致性      │  │
     │            │          │  └─────────────────────────────────────────┘  │
     │            │          │  ┌────────────┐ ┌────────────┐ ┌────────────┐  │
     │            │          │  │ 范围 Agent 1│ │ 范围 Agent 2│ │ 范围 Agent 3│  │
     │            │          │  │ 京城·朝堂   │ │ 江湖·武林   │ │ 北疆·边关   │  │
     │            │          │  │ 事件/因果   │ │ 事件/因果   │ │ 事件/因果   │  │
     │            │          │  │ 局部演进    │ │ 局部演进    │ │ 局部演进    │  │
     │            │          │  └──────┬─────┘ └──────┬─────┘ └──────┬─────┘  │
     │            │          │         └──────────────┼──────────────┘        │
     │            │          └────────────────────────┼───────────────────────┘
     └────────────────────────────────────────────────┼───────────────────────┘
                                                       ▼
                                            ┌─────────────────────┐
                                            │  共享上下文 (Context)   │
                                            │  当前场景、当前范围、    │
                                            │  已发生事件、时间/地点   │
                                            └─────────────────────┘
```

---

## 四、角色 Agent（独立演进）

每个重要角色一个 Agent，具备**独立的记忆库**，与其它 Agent **分开存储**、互不共享；仅通过编排器下发的共享上下文与回合结束后的同步写回间接获得“他人/世界”信息。

| 维度 | 说明 |
|------|------|
| **身份设定 / 人物设定** | 姓名、背景、性格标签、口头禅等；**须依据总的设定要求**（世界设定、类型、战力/等级等全局约束），与全局设定一致，可由设定研究 Agent 或世界层约束。 |
| **私有记忆库（分库存储）** | 该角色专属记忆，按子类分开存储、可分别检索，例如：**社交关系**（与其它角色的关系、信任/敌对、亲疏变化）；**不同时间线上发生的事件提炼**（该角色所知事件的摘要、按时间线或范围组织）；**情感变化**（对某人某事的情绪、重大事件带来的情感转折）；以及秘密、误解等。 |
| **目标与动机** | 短期目标（本场景想做什么）、长期目标（角色弧光），可随剧情更新 |
| **独立“思考”** | 每回合前：根据当前情境 + **自身记忆库**（关系、事件提炼、情感等）+ 目标，生成内心独白/决策理由，再生成对外言行 |
| **演进** | 经历重大事件后更新：记忆库各子类（关系、事件提炼、情感）、目标、甚至人物设定微调（创伤、成长） |

**记忆库隔离**：每个角色 Agent 只读写**自己的**记忆库；世界层、范围 Agent 各有自己的事件簿与状态存储，同样与角色记忆库分开。

**演进示例**：  
角色 A 原本“轻信他人”，在剧情中被背叛 → 世界/编排器记录事件 → 角色 A 的 Agent 在其**独立记忆库**中更新社交关系（对某人由信任→怀疑）、事件提炼（记录该次背叛）、情感变化（伤心、多疑），并可选更新人物设定中的性格标签（如增加“多疑”），后续对话与决策随之变化。

---

## 五、世界层：多个 Agent（世界 = 整个故事范围，多范围演进）

**世界** = 整个故事范围；其下可有**多个事件演进范围**，每个范围对应**至少一个世界层 Agent**。

### 5.1 世界协调者 Agent（可选）

当存在多条范围且范围间有联系时，可设一个**世界协调者**：

| 职责 | 说明 |
|------|------|
| **全局设定** | 时代、规则、地图、势力总览等，所有范围共享且不可冲突 |
| **统一时间轴** | 确保各范围事件在全局时间上可排序、可交叉 |
| **跨范围因果** | 处理“范围 1 发生的事影响范围 2”的登记与一致性 |
| **交叉事件** | 角色跨线、情报互通、大事件波及多范围时的协调 |

### 5.2 范围 Agent（每个事件演进范围一个）

每个**事件演进范围**（如京城线、江湖线、宫廷线）一个 **范围 Agent**，负责该范围内：

| 职责 | 说明 |
|------|------|
| **范围内世界状态** | 该范围的地点、势力、局部规则、当前时间点 |
| **范围内事件簿** | 仅本范围已发生事件（时间、地点、参与角色、结果），供检索与因果推理 |
| **范围内因果与一致性** | 新情节在本范围内是否与已有事件/设定冲突 |
| **范围内世界演进** | 本范围大事件后更新局部状态（如江湖线中某派覆灭、京城线中某案结案） |
| **旁白/环境** | 可输出本范围的场景描写、天气、环境，与角色台词区分 |

范围之间可**独立演进**（互不引用），也可**有联系**：通过世界协调者或编排器传递“跨范围事件摘要”，供另一范围 Agent 做约束或演进。

世界层 Agent（协调者 + 各范围 Agent）均不“扮演”具体角色，只维护设定与事件演进，并可选地生成非角色视角的叙述。**每个范围 Agent 拥有自己的事件簿与状态存储**，与其它范围、与各角色记忆库分开存储；协调者仅维护全局时间与跨范围摘要，不替代各范围的独立存储。

---

## 六、设定研究 Agent（特殊设定完整模型）

专门负责**设计并完善与故事配套的特殊设定**的独立 Agent，产出**完整、可落地的设定模型**（如战力体系、等级体系、境界、势力层级等），供世界层与角色层统一引用，保证全书设定一致、有据可依。

### 6.1 定位与输入输出

| 项目 | 说明 |
|------|------|
| **定位** | 设计期 / 开书前或设定迭代时调用，也可在写作过程中按需“补设定”；不参与每回合写戏，只产出**设定文档**与**结构化数据**。 |
| **输入** | 题材、类型、已有粗设定（如“修仙”“武侠”“都市异能”）、希望借鉴的参照（历史时期、同类作品名等）。 |
| **输出** | 一套**完整特殊设定**：战力/等级/境界定义、数值或层级表、进阶条件、与剧情/角色的挂钩方式等，写入全局世界设定或范围设定。 |

### 6.2 能力：联网搜索 + 分析已有作品

该 Agent 具备**联网搜索**与**文本分析**能力，用于收集与归纳资料，再生成配套设定：

| 能力 | 说明 |
|------|------|
| **联网搜索** | 按题材/类型检索：历史背景、武学体系、修仙境界、游戏等级设计、行业术语等；可检索百科、论坛、设定集、读者总结等，作为设定依据或参考。 |
| **分析历史小说** | 对经典/历史题材小说（或摘要、书评、设定整理）做结构化分析：提取时代考据、官职体系、礼仪习俗、战力参照等，用于历史向或仿古设定的严谨性与风格统一。 |
| **分析互联网小说** | 对同类型网文（或公开的设定帖、战力排行、等级表）做分析：归纳常见的等级命名、战力膨胀曲线、境界划分、金手指类型等，用于生成**符合类型惯例且可区分度清晰**的等级/战力体系。 |
| **完善战力、等级等** | 在以上信息基础上，产出**本作专属**的完整模型：例如境界层级表（含名称、大致战力区间、突破条件）、战力换算规则、与“范围/势力/角色”的绑定方式，便于世界 Agent 与角色 Agent 在写戏时一致引用。 |

### 6.3 产出示例（战力 / 等级）

- **战力体系**：定义“战力”在本作中的含义（如修为、武力值、综合评分），可选数值区间或档位（如 1–100 档位、或“同境九品”），以及跨境界/跨范围的换算或压制关系。  
- **等级/境界体系**：分层命名（如炼气→筑基→金丹…）、每层对应的大致能力描述、突破条件或代价，与剧情节点（如某范围大事件导致境界上限开放）的挂钩方式。  
- **其他配套设定**：势力层级、宝物/功法品阶、货币与资源等级等，与战力/等级形成统一命名与引用规范。

### 6.4 与其它组件的衔接

- **世界协调者 / 范围 Agent**：将设定研究 Agent 产出的**特殊设定文档**写入全局世界设定或各范围设定；写戏时由世界层按“当前范围 + 当前类型”提供约束（如本范围使用哪套境界名、战力区间）。  
- **角色 Agent**：角色卡或状态中引用“境界/等级/战力”等字段，其取值与进阶逻辑与设定研究 Agent 产出的模型一致，避免前后矛盾。  
- **编排器**：在“开书前”或“设定迭代”流程中调用设定研究 Agent；若写作中检测到未定义的新概念（如新境界、新势力层级），可触发一次设定研究 Agent 的补全请求。

### 6.5 设定作为共享基础规则与检索式注入

- **设定 = 世界共享基础规则记忆**：战力/境界/世界规则等设定是**全世界共享**的基础规则，不应停留在「一次生成的概要」；应与作者**多轮对话**（开书前设定阶段的 y/e/s 循环）不断细化——作者可用「s 补充说明」多轮细化、或「e 编辑」直接改 YAML，使设定从简到详、从概要到可落地的完整规则。设定研究 Agent 的初次产出视为**种子/概要**，后续通过对话与编辑逐步扩充每个设定项。
- **每条记忆都可能很长**：设定文档、角色私有记忆、范围事件簿等随剧情推进会持续增长；**所有 Agent 必须具备检索能力**，按需取用相关内容，**不得**每回合将全部设定或全部记忆全量导入 LLM 上下文，否则会超出模型上下文上限。
- **当前与后续约定**：角色记忆已通过 `retrieve_character_memory(..., events_limit/relations_limit)` 做截断与摘要；范围事件通过 `format_scope_events_snippet(..., k)` 只取最近 k 条。设定若在回合内注入到角色/范围 Agent，也应提供**设定检索**（如按主题/范围取相关段落或摘要），而非整份 YAML 全量塞入 prompt。

### 6.6 设定阶段与作者的按方向讨论流程

开书前设定阶段中，设定研究 Agent 与作者的交互支持**自由讨论**（可交叉多维度），**讨论结束后由 LLM 归纳到各设定方向**并提示作者；设定分类**不写死**，随作者需求与思路扩展；仅在作者明确完成设定时才结束整体初步设定。流程约定如下：

| 步骤 | 说明 |
|------|------|
| **1. 作者直接说出想法** | 作者输入想讨论的内容（如「战力与境界的关系」「引烬期下各战力等级」等），**不通过固定菜单选方向**；可自然涉及多个维度及其关系。 |
| **2. 自由讨论** | 设定 Agent 与作者**自由讨论**，每次答复可涉及多方向、关系与交叉问题；不在一开始锁定单一方向，以便处理综合话题。 |
| **3. 满意后归纳到各方向** | 作者输入「满意」后，由 LLM 根据整段讨论**判断涉及哪些设定方向**，并针对每个方向提炼 YAML，**归纳到各方向**；设定维度由已有设定与讨论内容动态扩展，**不写死**在配置或代码中。 |
| **4. 呈现归纳结果并询问** | 提炼后，**提示作者已归纳到的方向**并呈现更新后的整体设定摘要；主菜单询问：修改 / 继续讨论 / **保存进度** / 设定完成。 |
| **5. 持久化选型** | 每次咨询用户时提供**保存进度**选型（主菜单「p」、讨论中输入「保存」），将**所有输出内容**（设定摘要、完整讨论对话、当前状态）写入 `config/design_session.yaml`，便于后续启动时**增量续写**与恢复。 |
| **6. 明确完成才结束** | **只有作者明确表示「设定完成」或等价确认**时，设定阶段才结束。 |

**实现要点**：主循环区分「整体审阅」「按方向讨论」「编辑/补充」「保存进度」等分支；「按方向讨论」子循环使用自由对话，结束时由 LLM 将讨论归纳到多个方向并合并进 special；持久化模块记录摘要、菜单选择、完整讨论轮次与状态快照，供后续加载与增量交互。

### 6.7 交互主体、协创与自动化边界（L0 摘要）

- **统一对话主体（目标态）**：作者在与系统交互时，宜感知为**同一助手**在理解意图、查阅已有材料再回应；具体技术路径（分类、检索、阶段策略）见 L1/L2：[docs/specs/author-in-loop-spec.md](./docs/specs/author-in-loop-spec.md)、[docs/design/author-agent-harness.md](./docs/design/author-agent-harness.md)。  
- **协创与跳脱思路**：鼓励助手在适当时机提供联想与方案对比，但须**可配置、可关闭**，且不得替代作者在关键写回点的确认（与 §6.6、第七章一致）。  
- **超长篇全自动**：**默认产品定位**不承诺「单条诉求、无审阅、全自动产出百万字级长篇」；若未来单独提供此类**执行层**模式，须另立规约并与本条及作者在环条款显式协调。  
- **续写潜力评估（远期）**：计划中的能力是——基于当前已交流的设定、正文与持久化材料，**评估**在约定质量假设下后续**最多可自动续写多长（字数或章节）**并说明瓶颈；**现阶段不实现**，见 [docs/design/author-agent-harness.md](./docs/design/author-agent-harness.md) §7.1 与 [docs/specs/author-in-loop-spec.md](./docs/specs/author-in-loop-spec.md) §5。评估结论**不等于**默认自动写满该长度。

---

## 七、协作流程（单回合简化）

1. **编排器** 确定当前场景：**当前事件演进范围**、时间、地点、在场角色、上一幕结尾状态。
2. **世界层**：
   - **当前范围的范围 Agent** 根据本范围状态 + 本范围事件簿（+ 若需要则从协调者取跨范围摘要），给出场景约束与建议（可写/不可写）。
   - 若存在**世界协调者**且本回合涉及跨范围（如角色跨线、引用他线事件），由协调者提供全局一致性约束或交叉事件摘要。
3. **各角色 Agent**（在场角色）并行或按序：
   - 输入：当前情境 + 自身记忆 + 目标 + 世界/范围约束；
   - 输出：内心独白（可选） + 本回合的**言行**（对话/动作）。
4. **编排器** 收集所有角色的输出，做简单冲突检测，必要时裁决或要求某 Agent 重写。
5. **作者交互（第一次）**：本回合各 Agent 的运行结果（角色言行、范围事件摘要等）提交给**作者**审阅；作者可**修改与变更**内容，**同意后**才进入同步。
6. **世界层（同步）**：作者同意后——
   - **当前范围的范围 Agent** 将本回合结果写入**本范围事件簿**，更新本范围状态（若有），并做范围内一致性检查；
   - 若本回合产生**跨范围影响**，由**世界协调者**登记或同步到对应范围。
7. **作者确认（第二次）**：同步结束后，**需作者确认**，系统才将本回合结果**写回各 Agent 的私有记忆**（及关系、目标等演进）；确认后本回合结束。
8. **编排器** 将本回合叙述合成为段落，写入章节，并决定下一场景/回合（可切换事件演进范围）。

---

## 八、多角色“独立演进”的体现

- **信息不对称**：每个角色只看到自己的记忆 + 当前场景共享信息，因此可以有“读者知道、角色不知道”的张力。
- **目标冲突**：角色 A 与 B 的目标冲突时，由编排器协调二者输出，而不是强行统一成一个“作者视角”。
- **关系与记忆驱动**：角色对彼此的态度由“关系网 + 私有记忆”驱动，关系随事件演进（从陌生到信任/敌对等）。
- **性格弧光**：长期目标与性格标签可在关键节点由编排器或规则触发更新，实现角色成长或堕落。
- **作者在环**：每回合结束时，各 Agent 的运行结果先与作者交互（审阅、修改、同意），同意后才进行同步；同步结束后仍需作者确认，再写回各 Agent 记忆，保证作者对剧情与角色记忆的最终把控。

---

## 九、技术实现（概要）

产品与协作设计见上文；**纯技术实现框架与方案**（框架选型、配置与数据模型、初始化与回合制流程、存储与并发、代码结构、实现顺序）已单独成文，作为**写代码前的参考文档**：

→ **[TECH_IMPLEMENTATION.md](./TECH_IMPLEMENTATION.md)**

要点摘要：多 Agent 由配置驱动、可配置启用角色/范围/协调者/设定研究；推荐 LangGraph + LangChain/LlamaIndex；初始化后各 Agent 同时启动，回合内并行独立演进；回合结束时先与作者交互（审阅/修改/同意）再同步，同步后需作者确认才写回各 Agent 记忆，结果波及下一回合。

---

## 十、后续可扩展

- **读者/评论 Agent**：对生成段落做可读性、节奏、伏笔检查。
- **类型与风格 Agent**：统一文风、类型惯例（如悬疑、言情）。
- **大纲/节拍 Agent**：负责高层次的章节规划与情节点，指导“当前要推进什么”。

如果你愿意，下一步可以在本仓库里按“编排器 / 角色 Agent / 世界 Agent / 共享上下文”拆成模块，并从一个最小可运行的单场景、双角色 demo 开始实现。
